# docs/legacy

Pre-CLAUDE.md exploration docs kept for the thesis appendix and historical context.

**Canonical project state lives in [`CLAUDE.md`](../../CLAUDE.md)**. The files in this directory predate it and may contradict each other; do not treat any of them as current ground truth.

## Overlapping pairs

These look like duplicates but have **different content** (different LLM passes / framings of the same topic). Pick by depth:

- `magic-box-first-trial-findings.md` (28 KB, 772 lines) — fuller treatment, leads with executive conclusion
- `magic_box_first_trial_findings.md` (22 KB, 684 lines) — terser variant, leads with "1. Bottom line"
- `deep-research-report.md` (21 KB, 256 lines) — full report
- `deep-research-report_current_State.md` (16 KB, 193 lines) — narrower follow-up scoped to "current state"

## Project-state docs (all stale, listed by recency of feel)

- `corpusagent2_project_export_summary.md` — broadest snapshot
- `REPO_STATE_FOR_CHATGPT_PRO.md` — repo-shape dump aimed at a different LLM
- `claude_current_state.md` — earlier Claude session export
- `current_state_current_date.txt` — same idea, plain text

## Other artifacts

- `corpusagent_2_general_chatbot_reframe.md` — the "general chatbot" pivot proposal that became the magic-box framing
- `codex history.txt` — raw Codex session transcript
- `cloudflare_setup.txt`, `NEXT_STEPS_CLOUDFLARED_GITHUB_PAGES.txt` — deployment notes; production paths are now in `docs/deployment.md` and `docs/dev/cpu-vm-claude.md`

When in doubt: read CLAUDE.md, not these.
