# Keep GitHub's language bar honest. Almost all the byte weight in this repo is
# generated HTML (repo-state dumps, the graphify visualisation, pytest cache),
# which otherwise buries the Python the project is actually made of.

graphify-out/**  linguist-generated=true
web_old_apple/** linguist-vendored=true
